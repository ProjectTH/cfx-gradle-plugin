package io.github.projectth.cfx.gradle.plugins

import org.gradle.api.Plugin
import org.gradle.api.Project

abstract class CfxGradlePluginWrapper : Plugin<Project> {
    override fun apply(project: Project) {
    }
}
